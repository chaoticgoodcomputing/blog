{
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "name": "blog",
  "sourceRoot": ".",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run site:serve"
        ],
        "cwd": ".",
        "parallel": false
      }
    },
  }
}
